name: 'ecommerce_dbt'
version: '1.0'
config-version: 2

profile: 'default'

model-paths: ["models"]

models:
  ecommerce_dbt:
    staging:
      +materialized: view

    marts:
      +materialized: table